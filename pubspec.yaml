name: sathachlaixe
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  equatable: ^2.0.0
  go_router: ^15.0.0
  flutter_svg: ^2.0.17
  morphable_shape: ^2.0.0
  shared_preferences: 2.5.3
  logging: ^1.3.0
  bloc: ^9.0.0
  provider: ^6.1.4

  exam_local_data:
    path: packages/exam_local_data
  exam_repository:
    path: packages/exam_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/drawables/
